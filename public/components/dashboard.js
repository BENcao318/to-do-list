$(() => {

  const $dashBoardContents = $(`
    <div class="container">
      Contents
    </div>
  `);

  window.$dashBoardContents = $dashBoardContents;

  window.dashBoardContents = {};
})